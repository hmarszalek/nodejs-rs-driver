# The cluster itself lives in `scylla-cluster.yml`; this file only pulls it in with
# the ScyllaDB version from `scylla_version.env`.
include:
  - path: scylla-cluster.yml
    env_file: ../scylla_version.env
